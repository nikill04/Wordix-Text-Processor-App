import dza, {a, b, d} from './module2.mjs'    // ==> You can write "dza" or any other as default one will imported here. But a, b and d should only as a, b and d respectively only as they are not defaultly being imported here. And we use '{}' when we are not by default importing here

console.log(dza)
console.log(b)
console.log(d)

// For running in console, type node ./src/myProject/myProgress_6/module1.mjs 